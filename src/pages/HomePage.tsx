//dependencies
import { FC } from "react";

//imports
import Home from "components/Home";

const HomePage: FC = () => {
  return (
    <div>
      <Home />
    </div>
  );
};

export default HomePage;
